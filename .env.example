# Required: Gemini API key (free tier available)
GEMINI_API_KEY=your_gemini_api_key_here

# Recommended: Make.com webhook URL (free: 1,000 operations/month)
# 1. Create a Make.com account
# 2. Create a scenario with webhook trigger
# 3. Add your webhook URL below
MAKE_WEBHOOK_URL=https://hook.make.com/your-unique-webhook-url

# Not recommended: IFTTT Webhook key (Twitter/X integration requires paid tier)
# IFTTT_KEY=your_ifttt_webhook_key

# Port for local web UI
PORT=3000

# Optional: Buffer API credentials (only for paid plans)
# BUFFER_ACCESS_TOKEN=your_buffer_token_here
# BUFFER_PROFILE_ID=your_profile_id_here
